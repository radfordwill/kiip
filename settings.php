<?php

// If this file is called directly, abort.
if ( !defined( 'WPINC' ) ) {
	die;
}

define( 'KIIP_FOR_WP_VERSION', '1.0.0' );

include_once( plugin_dir_path( __FILE__ ) . 'functions.php' );

//if ( !is_admin() ) {		

//}
